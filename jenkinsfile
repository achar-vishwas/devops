pipeline{
    agent any 
    
    stages{
        stage('System detail'){
            steps{
                sh '''
                echo "system details"
                uname -a
                '''
            }
        }
        
        stage('Memory details'){
            steps{
                sh '''
                echo "memory detail"
                free -h
                '''
            }
        }
    }
}
